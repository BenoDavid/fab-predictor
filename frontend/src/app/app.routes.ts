import { Routes } from '@angular/router';
import { PredictComponent } from './components/predict-component/predict-component';

export const routes: Routes = [{ path: '', component: PredictComponent }];
